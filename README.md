# Careful Life

## Development

- `npm install`
- `npm run dev`

## Production Build

- `npm run build`
- `npm run preview`

Image assets are optimized automatically during `npm run build` via `vite-plugin-image-optimizer`.